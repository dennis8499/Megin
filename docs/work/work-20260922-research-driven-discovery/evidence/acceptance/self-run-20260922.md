# 自行驗收紀錄：研究驅動需求探索

- work_id: `work-20260922-research-driven-discovery`
- acceptance_version: `acceptance-1`
- actor: assistant（依使用者指示先自行實作與驗收一輪）
- acceptance_basis: 使用者明確指示「請先自己實做一遍，如果有問題我會再開新案」，授權以本次自行重播作為
  `acceptance-1` 的驗收方式
- snapshot: `feature/work-20260922-research-driven-discovery`，`HEAD` `6afd0e817bb22894aac8d801df60a81cfdd7ff4c` 加上目前已審查的未提交工作樹
- workspace: `C:\Users\denni\OneDrive\Desktop\新增資料夾\Megin`
- date: `2026-09-22`
- result: `passed`

## 方法

依 `tests/requirements-discovery/fixtures/` 的案例逐項重播目前的協定、入口 Skill、探索 Skill、規劃 Skill、
需求主檔與工作紀錄。先執行材料檢查、反例測試、規則測試、語言政策、封裝驗證與 `git diff --check`，再檢查
每個使用者可觀察情境的路由、等待狀態、來源邊界、能力層級、revision 與只讀限制。這是本次快照的自行重播，
依使用者明確指示作為 `acceptance-1` 的驗收方式；不宣稱已完成新舊模型對照，
`tests/requirements-discovery/results/result-template.md` 的模型評測仍為 `status: not-run`。

## 情境結果

| 情境 | 自行操作與預期 | 觀察證據 | 結果 |
| --- | --- | --- | --- |
| `REQ-DISC-001` | 用 Quartz fixture 提出完整支援陌生外部框架；先研究來源與版本、盤點能力，再問一個前提已具備的高影響決策，分開上游與應用層。 | `requirements-discovery-protocol.md` 的研究觸發、能力覆蓋與決策選題；`SRC-QUARTZ-*`；`CAP-001` 至 `CAP-004`。 | pass |
| `REQ-DISC-002` | 使外部來源不可讀；記錄限制與重大未知，保持 `phase: requirements` / `status: awaiting_user`，不補造來源。 | 協定的來源失效規則；`fixtures/quartz/README.md`；`features/requirements-discovery.feature` 的 `REQ-DISC-002`。 | pass |
| `REQ-DISC-003` | 用 explicit-small fixture 提供完整用途、受眾、範圍、排除與驗收；不做無關研究、不追加形式問題，可交接 planning。 | 協定的明確小改動例外與完成條件；`fixtures/explicit-small/README.md`；`SCN-DISC-003`。 | pass |
| `REQ-DISC-004` | 一次提供用途、負載、權限與資料生命週期；全部吸收並更新需求，不重問已回答問題。 | 協定的決策選題與回答吸收規則；`SCN-DISC-004`。 | pass |
| `REQ-DISC-005` | 提供衝突答案或延後會改變核心介面的事項；保留矛盾、能力影響與規劃阻礙，不交接 planning。 | 協定的完成條件與阻礙規則；`SCN-DISC-005`。 | pass |
| `REQ-DISC-006` | 即使問題很多仍保留會改變用途、邊界或驗收的未知；不以題數、文件或 `ready_for_planning` 放行。 | 協定的明確禁止項；`SCN-DISC-006`。 | pass |
| `REQ-DISC-007` | 用 resume fixture 恢復缺欄位舊紀錄或改變範圍；重新檢查來源／能力／決策／缺口並遞增 `requirements_revision`，使舊計畫失效。 | `fixtures/resume/README.md`；`workflow-record.md`；協定的恢復規則；`SCN-DISC-007`。 | pass |
| `REQ-DISC-008` | 改用 Redis 廣泛需求；引用 Redis 官方快照，分開能力與整合層，不依賴 Quartz 文字。 | `SRC-REDIS-001`、`SRC-REDIS-002`；`requirements.md` 的 `CAP-002` 與 `SCN-DISC-008`；`fixtures/redis/README.md`。 | pass |
| `REQ-DISC-009` | 以純解釋或未核准計畫輸入路由；不改產品、不暫存、不提交、不更新正式知識，開發仍需核准與 feature branch。 | `megin/SKILL.md` 的 acceptance/delivery 邊界；`branch-policy.md`；`SCN-DISC-009`。 | pass |
| `REQ-DISC-010` | 執行材料檢查器並餵入缺來源、重複 ID、失效參照等反例；合法材料通過、反例失敗，且不判斷自然語言完整性。 | `check_materials.py`、`test_materials.py`；verification 與 fresh review r5 的 exit 0 輸出。 | pass（automatic） |
| `REQ-DISC-011` | 只說完整支援某項技術而未指出產品、框架、版本或整合環境；不猜來源或能力，只問一個方向問題並等待。 | 協定的研究對象不明分支；`fixtures/unknown-target/README.md`；`SCN-DISC-011`。 | pass |

## 結論與限制

自行重播未發現規則、參照或路由缺陷；並修正了工作紀錄中與 `phase: acceptance` 不一致的「尚未進入
acceptance」殘留文字。使用者已授權這輪重播作為本次 `acceptance-1`，但它仍不能取代固定模型、來源與完整
對話的後續評測，也不宣稱對話行為已被實證改善。
