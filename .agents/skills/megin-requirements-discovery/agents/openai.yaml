interface:
  display_name: "Megin requirements discovery"
  short_description: "Clarify scope and acceptance before coding"
  default_prompt: "Use $megin-requirements-discovery to clarify this repository change and define its acceptance scenarios."
policy:
  allow_implicit_invocation: true
