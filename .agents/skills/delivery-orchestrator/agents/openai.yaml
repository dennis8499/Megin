interface:
  display_name: "Delivery Orchestrator"
  short_description: "交付軟體變更的統一入口與人工 Gate 流程"
  default_prompt: "使用 $delivery-orchestrator 作為唯一 mutating SDLC 入口，建立或續接隔離 workspace，依 phase 自動路由需求、規劃、實作與 fresh review，並保留人工 Gate。"

policy:
  allow_implicit_invocation: true
