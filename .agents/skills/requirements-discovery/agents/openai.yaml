interface:
  display_name: "需求探索"
  short_description: "探索經統一交付入口路由的可驗收需求"
  default_prompt: "使用 $delivery-orchestrator 統一入口路由 $requirements-discovery；取得 requirements 階段授權後，以證據先行、一次一題的 frontier 訪談形成可確認需求。"

policy:
  allow_implicit_invocation: true
