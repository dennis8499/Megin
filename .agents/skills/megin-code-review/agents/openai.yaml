interface:
  display_name: "Megin code review"
  short_description: "Review the approved snapshot independently"
  default_prompt: "Use $megin-code-review to perform a fresh read-only review of the approved snapshot."
policy:
  allow_implicit_invocation: true
